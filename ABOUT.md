# About

**buzz-desktop-headless** runs the Buzz Desktop GUI client on headless Linux
using Xvfb + a lightweight window manager + x11vnc + noVNC.

It does **not** replace the Buzz relay or ACP agents. Those remain separate
server processes. This package is the portable “desktop on a server” shell.
